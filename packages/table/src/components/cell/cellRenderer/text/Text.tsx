import * as React from 'react';

import { StyledTableCellRendererText } from './StyledCellRendererText';

export const RenderCellContentText = ({ value }) => {
  debugger;
  return <StyledTableCellRendererText>{value}</StyledTableCellRendererText>;
};
