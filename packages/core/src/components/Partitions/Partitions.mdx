import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './Partitions.stories';

<Meta of={Stories} />

# Partitions

## Overview

The `Partitions` component renders a set of partitions with different sizes and colors.
Each partition can have a tooltip that is displayed when the user hovers over it.

## How to import

<Source
  code={`
import { Partitions } from '@devoinc/genesys-ui';
`}
/>

## Basic usage

<Canvas of={Stories.Base} />

## API

<ArgTypes of={Stories} />
