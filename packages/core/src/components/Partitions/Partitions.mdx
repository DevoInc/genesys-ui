import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';
import { Heading } from '../../../../../.storybook/docs/blocks';

import * as Stories from './Partitions.stories';

<Meta of={Stories} />

<Heading.H1>Partitions</Heading.H1>

<Heading.H2>Overview</Heading.H2>

The Partitions component is used to render a set of partitions with varying sizes and colors.
Each partition can be assigned a tooltip that is displayed when the user hovers over it,
providing additional information or context.

<Heading.H2>How to import</Heading.H2>

<Source
  code={`
import { Partitions } from '@devoinc/genesys-ui';
`}
/>

<Heading.H2>Basic usage</Heading.H2>

<Canvas of={Stories.Base} />

<Heading.H2>Props</Heading.H2>

<ArgTypes of={Stories} />
