import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';
import { Heading } from '../../../../../.storybook/docs/blocks';

import * as Stories from './SelectControl.stories';
import * as CaseStories from './SelectControl.cases.stories';

<Meta of={Stories} />

# SelectControl

## Overview

The Select component provides a way for users to select an option from a dropdown list.
It allows users to choose from a set of predefined options, which can be customized to suit the needs of the application.
The Select component is implemented using the [react-select](https://react-select.com/home) library, which provides a flexible and powerful API for creating customized dropdown menus.

## How to import

<Source
  code={`
import { SelectControl } from '@devoinc/genesys-ui';
`}
/>

## Basic usage

<Canvas of={Stories.Base} />

## Cases

### Single option

<Canvas of={CaseStories.SingleOption} />

### Single with option groups

<Canvas of={CaseStories.SingleWithOptionsGroups} />

### Multiple basic

<Canvas of={CaseStories.MultipleBasic} />

### Multiple sortable

<Canvas of={CaseStories.MultipleSortable} />

### Multiple creatable (custom new options)

<Canvas of={CaseStories.MultipleCreatable} />

### Multiple 'Select all' mode

<Canvas of={CaseStories.MultipleSelectAll} />

### Multiple fixed options and sortable

<Canvas of={CaseStories.MultipleFixedOptions} />

### With options virtualization for large sets

<Canvas of={CaseStories.VirtualizedOptions} />

## Props

<ArgTypes of={Stories} />

## Related components

- [Select](?path=/docs/components-form-select--docs)
