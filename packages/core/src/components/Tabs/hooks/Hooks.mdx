import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from './Hooks.stories';

<Meta of={Stories} />

<H1>Tabs Hooks</H1>

Hook to build a TabsItem list.
Given an array of tabs config and common config props,
it returns an array of children to be rendered by the TabsContainer.

<H2>UseTabsConfig</H2>

<Canvas of={Stories.Base} />

<H4>Parameters</H4>

<Source
  code={`
{ 
  active, // default active tab
  href, // href
  iconId, // iconId
  size, // size
  state, // state
  tabs, // array of tabs
  target, // target
  wide, // wide
}
`}
/>

<H4>Usage</H4>

<Source
code={`
import { Tabs, useTabsConfig } from '@devoinc/genesys-ui';

const tabs = [
{ label: 'Tiny' },
];
const tabsConfig = useTabsConfig({ tabs });

const children = tabsConfig.map((tab, idx) => (

<Tabs.Item {...tab} key={idx} />
)) as never;

return (

<Tabs>
  {children}
  <Tabs.Aside>Aside area</Tabs.Aside>
</Tabs>
);

`}
/>
