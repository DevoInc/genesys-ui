import { BASE_CLASS_NAME_PREFIX } from '../../constants';

export const WRAP_CLASS_NAME_BASE = `${BASE_CLASS_NAME_PREFIX}wrap`;
export const WRAP_ITEM_CLASS_NAME_BASE = `${WRAP_CLASS_NAME_BASE}__item`;
