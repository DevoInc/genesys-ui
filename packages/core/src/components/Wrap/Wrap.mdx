import { Meta, Canvas, ArgTypes, Source, Markdown } from '@storybook/blocks';

import * as Stories from './Wrap.stories';
import spacingProps from '../../../stories/partials/spacingProps.md?raw';

<Meta of={Stories} />

# Wrap

## Overview

Use `Wrap` to create a stacked group of children components distributed
in columns and rows. Each child can be separated horizontally using
the prop `hSpacing` and vertically using `vSpacing`.

## How to import

<Source code={`import { Wrap } from '@devoinc/genesys-ui';`} />

## Basic usage

You only have to add children to the component and wrap them.

<Canvas of={Stories.Base} />

## Using Wrap.Item

We can use `Wrap.Item` component in a flex structure wrapping children to assign flex,
order, and align-self CSS styles to each child component. It's only necessary when we need
those specific styles for any child.

Here is an example that demonstrates different CSS flex and align-self
settings for each child component of a Flex component:

<Canvas of={Stories.WrapAndWrapItems} />

## childrenByRow

You can use prop `childrenByRow` to define a specific number of columns with the same width for each child.
In this example you can see four children by row:

<Canvas of={Stories.ChildrenByRow} />

## Using the as property

The `as` property allow these components to be rendered as any HTML tag: 'div', 'li', 'section'... etc.

If you want to use the `Wrap` component rendered with a different HTML tag, you can use the `as` property.
It allows these components to be rendered as any HTML tag, such as div, li, section, and so on.

<Source
  code={`
import * as React from 'react';
import { Wrap } from '@devoinc/genesys-ui';

const MyWrapCmp = () => (

<Wrap as={'ul'}>
  <Wrap.Item as={'li'}>Child 1</Wrap.Item>
  <Wrap.Item as={'li'}>Child 2</Wrap.Item>
  <Wrap.Item as={'li'}>Child 3</Wrap.Item>
</Wrap>
); `} />

<Markdown>{spacingProps}</Markdown>

## Props

<ArgTypes of={Stories} />
