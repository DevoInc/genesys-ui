import { Meta, Canvas, ArgTypes, Source, Markdown } from '@storybook/blocks';
import { Heading } from '../../../../../.storybook/docs/blocks';

import * as Stories from './Wrap.stories';
import spacingProps from '../../../stories/partials/spacingProps.md?raw';
import { StyledLayoutContentHelper } from '../../../stories/components/styled';

<Meta of={Stories} />

<Heading.H1>Wrap</Heading.H1>

<Heading.H2>Overview</Heading.H2>

Use `Wrap` to create a stacked group of children components distributed
in columns and rows. Each children can be separated horizontally using
the prop `hSpacing` and vertically using `vSpacing`.

<Heading.H2>How to import</Heading.H2>

<Source
  code={`
import { Wrap } from '@devoinc/genesys-ui';
`}
/>

<Heading.H2>Basic usage</Heading.H2>

<Canvas of={Stories.Base} />

<Heading.H2>Cases</Heading.H2>

<Heading.H3>childrenByRow</Heading.H3>

You can use prop `childrenByRow` to define columns with the same width for each child:

<Source
  code={`
<Wrap childrenByRow={4}>
  ...
</Wrap>
`}
/>

<Heading.H3>Using the as property</Heading.H3>

The `as` property allow these components to be rendered as any HTML tag:
'div', 'li', 'section'... etc.

If you want to use the `Flex` component rendered with a different HTML tag, you can use the `as` property.
It allows these components to be rendered as any HTML tag, such as div, li, section, and so on.

<Source
  code={`
import * as React from 'react';
import { Flex, FlexItem } from '@devoinc/genesys-ui';

const MyFlexCmp = () => (

{' '}

<Flex as={'ul'}>
  <FlexItem as={'li'}>Child 1</FlexItem>
  <FlexItem as={'li'}>Child 2</FlexItem>
  <FlexItem as={'li'}>Child 3</FlexItem>
</Flex>
); `} />

<Markdown>{spacingProps}</Markdown>

<Heading.H2>Props</Heading.H2>

<ArgTypes of={Stories} />
