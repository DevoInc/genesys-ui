import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './IconButtonStop.stories';

<Meta of={Stories} />

# IconButtonStop

It is a special type of IconButton that is used to stop
processes. It is typically used in situations where the user needs to quickly
halt an action or process.

The `IconButtonStop` is usually included in the regular flow of the DOM.

## How to import

<Source code={`import { IconButtonStop } from '@devoinc/genesys-ui';`} />

## Basic usage

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories} />
