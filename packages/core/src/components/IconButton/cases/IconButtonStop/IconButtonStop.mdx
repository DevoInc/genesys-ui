import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './IconButtonStop.stories';

<Meta of={Stories} />

<H1>IconButtonStop</H1>

<H2>Overview</H2>

The IconButtonStop is a special type of IconButton that is used to stop processes. It is typically used in situations
where the user needs to quickly halt an action or process. The IconButtonStop is usually included in the regular flow of the DOM.

<H2>How to import</H2>

<Source
  code={`
import { IconButtonClose } from '@devoinc/genesys-ui';
`}
/>

<H2>Basic usage</H2>

<Canvas of={Stories.Base} />

<H2>Props</H2>

<ArgTypes of={Stories} />
