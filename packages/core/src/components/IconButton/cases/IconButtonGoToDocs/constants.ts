import type { TButtonSize } from '../../../Button';
import { TIconSize } from '../../../Icon/declarations';

export const GO_TO_DOCS_SIZE_ICON_MAP: { [key in TButtonSize]: TIconSize } = {
  xxs: 'xxs',
  xs: 'xxs',
  sm: 'xs',
  md: 'sm',
  lg: 'md',
};
