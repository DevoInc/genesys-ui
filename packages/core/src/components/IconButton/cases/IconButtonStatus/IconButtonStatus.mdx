import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './IconButtonStatus.stories';

<Meta of={Stories} />

# `IconButtonStatus`

## Overview

The `IconButtonButtonStatus` is a special type of `IconButton` used to trigger
information pop-up blocks, typically in help contexts, to expand information
about a functionality.

## How to import

<Source code={`
import { IconButtonClose } from '@devoinc/genesys-ui';
`} />

## Basic usage

<Canvas of={Stories.Base} />

## Props

<ArgTypes of={Stories} />
