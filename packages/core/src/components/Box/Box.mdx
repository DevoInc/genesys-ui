import { Meta, Canvas, Source, Markdown, ArgTypes } from '@storybook/blocks';

import * as Stories from './Box.stories';
import * as CaseStories from './Box.cases.stories';

import spacingProps from '../../../stories/partials/spacingProps.md?raw';

<Meta of={Stories} />

# [Box](#box)

## [Overview](#overview)

Use `Box` component to wrap a functional component and apply
layout styles to it: margin, position, padding, overflow... etc.
Definitely it provides a shorthand way to pass layout styles via props to
another component.

## [How to import](#how-to-import)

<Source
  code={`
import { Box } from '@devoinc/genesys-ui';
`}
/>

## [Basic usage](#basic-usage)

<Canvas of={Stories.Base} />

## [Cases](#cases)

### [as](#as)

Likewise, its `as` property allow this component to be rendered as any HTML tag:
'div', 'li', 'section'... etc.

<Source
  code={`
import * as React from 'react';
import { Box } from '@devoinc/genesys-ui';

const MyFlexCmp = () => <Box as={'span'}>Box content</Box>;
`}
/>

This is an example with `Button` and `Badge`. We place the `Badge` in absolute
way, relative to the `Button`:

<Canvas of={CaseStories.PlacingFunctionalComponents} />

This is another typical use case: to generate vertical scroll in a content.
A group of `Paragraph` components wrapped by a `Box` with specific 'max-height'
and 'overflow' to auto:

<Canvas of={CaseStories.Scrolled} />

<Markdown>{spacingProps}</Markdown>

## [API](#api)

<ArgTypes of={Stories} />
