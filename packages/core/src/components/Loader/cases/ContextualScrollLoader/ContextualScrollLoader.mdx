import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './ContextualScrollLoader.stories';

<Meta of={Stories} />

# Loader.ContextualScroll

## Overview

The `Loader.ContextualScroll` is a specific type of `Loader` component designed to be used with the infinite scroll functionality, such as in a Table that loads new rows from the backend while scrolling.
It provides a temporary overlay that is positioned relative to a container and offers the same options for loading indicators as the Contextual Loader - including both spinner and progress bar types.

## How to import

<Source
  code={`
import { Loader } from '@devoinc/genesys-ui';
`}
/>

## Basic usage

<Canvas of={Stories.Base} />

## Props

<ArgTypes of={Stories} />

## Related components

- [Loader](?path=/docs/components-core-feedback-loader--docs)
- [Loader.Global](?path=/story/components-core-feedback-loader-globalloader--docs)
- [Loader.Contextual](?path=/docs/components-core-feedback-loader-contextualloader--docs)
