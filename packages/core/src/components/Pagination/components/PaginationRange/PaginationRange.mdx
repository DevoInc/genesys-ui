import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './PaginationRange.stories';

<Meta of={Stories} />

# Range

The selector to define the range of items per page in the `Pagination`
component.

## How to import

You have to use the component as dot notation of the main component as
`Pagination.Range`.

<Canvas of={Stories.Base} />

### Props

<ArgTypes of={Stories} />

