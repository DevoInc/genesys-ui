import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './PaginationNav.stories';

<Meta of={Stories} />

# Nav

The page navigation in the `Pagination` component.

## How to import

You have to use the component as dot notation of the main component as
`Pagination.Nav`.

<Canvas of={Stories.Base} />

