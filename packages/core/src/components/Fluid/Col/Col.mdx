import { Meta, Canvas, ArgTypes, Source, Markdown } from '@storybook/blocks';

import * as Stories from './Col.stories';

<Meta of={Stories} />

# Col

Layout component that represents a column in a grid system.
It takes a number of props that can be used to customize its behavior and
appearance.

By using the Col component along with other components such as Row and
Container, one can easily create flexible and responsive layouts that adapt to
different screen sizes and device types.

## How to import

<Source code={`import { Col } from '@devoinc/genesys-ui';`} />

## Basic usage

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories} />

## Related components

- [Container](?path=/docs/components-layout-fluid-container--overview)
- [Row](?path=/docs/components-layout-fluid-row--overview)
