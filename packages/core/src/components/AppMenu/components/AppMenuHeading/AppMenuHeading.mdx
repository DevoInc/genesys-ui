import { Meta, Canvas, ArgTypes, Source } from '@storybook/addon-docs/blocks';

import * as Stories from './AppMenuHeading.stories';

<Meta title="Components/Navigation/AppMenu/Components/AppMenuHeading/Overview" />

# AppMenu.Heading

This component is used to define a menu heading to group app menu items.

## How to import

<Source code={`import { AppMenu } from '@devoinc/genesys-ui';`} />

## Basic usage

This component is defined with dot notation as `AppMenu.Heading` and it should be a child of a `AppMenu` component.

<Canvas of={Stories.Playground} />

## Props

<ArgTypes of={Stories} />
