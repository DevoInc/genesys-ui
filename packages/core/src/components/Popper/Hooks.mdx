import { Meta, Source } from '@storybook/blocks';
import { Heading } from '../../../../../.storybook/docs/blocks';

<Meta title="Components/Core/Popper/Hooks/Docs" />

<Heading.H1>Hooks</Heading.H1>

<Heading.H2>usePopper</Heading.H2>

In case you require a specific behavior, you can use the hooks offered by the library
directly in your code. You can refer to the documentation of [React Popper](https://popper.js.org/react-popper/v2/)
for further details.

Below is a brief example showcasing its use:

<Source
  code={`
import * as React from 'react';
import { usePopper } from 'react-popper';

const Example = () => {
const [referenceElement, setReferenceElement] = React.useState(null);
const [popperElement, setPopperElement] = React.useState(null);
const [arrowElement, setArrowElement] = React.useState(null);
const { styles, attributes } = usePopper(referenceElement, popperElement, {
modifiers: [{ name: 'arrow', options: { element: arrowElement } }],
});

return (

<>
  <button type="button" ref={setReferenceElement}>
    Reference element
  </button>
  <div ref={setPopperElement} style={styles.popper} {...attributes.popper}>
    Popper element
    <div ref={setArrowElement} style={styles.arrow} />
  </div>
</>
); }; `} />
