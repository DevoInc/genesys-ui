import { Meta, Canvas, ArgTypes, Source } from '@storybook/addon-docs/blocks';

import * as stories from './FormButtons.stories';

<Meta title="Components/Form/Form/Components/FormButtons/Overview" />

# Form.Buttons

Component to create a row with a buttons block and a helper block in the opposite side.

### Basic usage

Defined with dot notation as `Form.Buttons`. This component should be used only as a
child of a `Form` or `Form` based component.

<Canvas of={stories.Playground} />

### Props

<ArgTypes of={stories} />
