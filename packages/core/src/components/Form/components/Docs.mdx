import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as FormStories from './FormButtons/FormButtons.stories';
import * as GroupStories from './FormGroup/FormGroup.stories';
import * as ItemStories from './FormItem.stories';
import * as LegendStories from './FormLegend/FormLegend.stories';

<Meta of={FormStories} />

# Components

Use each component as a dot notation component.

<Source
  code={`import { Form } from '@devoinc/genesys-ui';
<Form.Group />
<Form.Buttons />
<Form.Item />
`}
/>

## Form.Buttons

Component to create a row with a buttons block and a helper block in the
opposite side.

### Basic usage

<Canvas of={FormStories.Buttons} />

### Props

<ArgTypes of={FormStories} />

## Form.Group

Component to create an HTML fieldset and distribute the form groups and fields
inside it.

### Basic usage

<Canvas of={GroupStories.Base} />

### Collapsable

<Canvas of={GroupStories.Collapsable} />

### Props

<ArgTypes of={GroupStories} />

## Form.Item

Component to get specific distribution for each child of a Form or FormGroup.

### Basic usage

<Canvas of={ItemStories.Item} />

### Props

<ArgTypes of={ItemStories} />

## Form.Legend

Component that provides a legend to the form.

### Basic usage

<Canvas of={LegendStories.Legend} />

### API

<ArgTypes of={LegendStories} />
