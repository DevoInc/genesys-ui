import { Meta, Canvas, ArgTypes, Source, Markdown } from '@storybook/blocks';
import { H1, H2, H3, H4 } from '../../../../../.storybook/docs/blocks';

import spacingProps from '../../../stories/partials/spacingProps.md?raw';
import * as Stories from './FlexItem.stories';
import * as FlexStories from '../Flex/stories/Flex.stories';

<Meta of={Stories} />

<H1>FlexItem</H1>

<H2>Overview</H2>

To achieve specific layout behaviors for individual child components in
Flex container components such as Flex, HFlex, VFlex, and Wrap, you can use FlexItem to wrap each child.

<H2>How to import</H2>

<Source
  code={`
import { FlexItem } from '@devoinc/genesys-ui';
`}
/>

<H2>Basic usage</H2>

<Canvas of={Stories.Base} />

To apply spacing and flex styles such as flex, align-self, order, width, and margin, you can use FlexItem props.

<Canvas of={FlexStories.FlexAndFlexItems} />

For more information on using FlexItem components
as children of Flex container components,
refer to the Structure section in [Flex](?path=/docs/components-core-layout-flex-docs--docs).

## [Cases](#cases)

### [Using the as property](#using-the-as-property)

The `as` property allow these components to be rendered as any HTML tag:
'div', 'li', 'section'... etc.

If you want to use the `Flex` component rendered with a different HTML tag, you can use the `as` property.
It allows these components to be rendered as any HTML tag, such as div, li, section, and so on.

<Source
  code={`
import * as React from 'react';
import { Flex, FlexItem } from '@devoinc/genesys-ui';

const MyFlexCmp = () => (

{' '}

<Flex as={'ul'}>
  <FlexItem as={'li'}>Child 1</FlexItem>
  <FlexItem as={'li'}>Child 2</FlexItem>
  <FlexItem as={'li'}>Child 3</FlexItem>
</Flex>
); `} />

<Markdown>{spacingProps}</Markdown>

<H2>Props</H2>

<ArgTypes of={Stories} />

<H2>Related components</H2>

- [Flex](?path=/docs/components-core-layout-flex-flex--docs)
- [HFlex](?path=/docs/components-core-layout-flex-hflex--docs)
- [VFlex](?path=/docs/components-core-layout-flex-vflex--docs)
