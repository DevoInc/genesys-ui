import { Meta, Canvas, ArgTypes, Source } from '@storybook/blocks';

import * as Stories from './Stepper.stories';
import * as CaseStories from './Stepper.cases.stories';

<Meta of={Stories} />

# Stepper

## Overview

The purpose of the Stepper component is to provide a visual representation of a multi-step process.
The component can display the status of each step, indicating whether it is completed, in progress, or disabled, and allows the user to navigate between steps using buttons.

## How to import

<Source
  code={`
import { Stepper } from '@devoinc/genesys-ui';
`}
/>

## Basic usage

<Canvas of={Stories.Base} />

## Cases

### Navigation between steps

More complex usage of the Stepper component, where the state of the steps is managed by the parent component.
It demonstrates how to handle navigation between steps.

<Canvas of={CaseStories.WithNavigation} />

## Props

<ArgTypes of={Stories} />
