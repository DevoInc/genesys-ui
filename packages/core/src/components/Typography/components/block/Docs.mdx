import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as BlockQuoteStories from './BlockQuote.stories';
import * as CaptionStories from './Caption.stories';
import * as CodeStories from './Code.stories';
import * as HeadingStories from './Heading.stories';
import * as LeadStories from './Lead.stories';
import * as ListStories from './List.stories';
import * as ListItemStories from './ListItem.stories';
import * as ParagraphStories from './Paragraph.stories';

<Meta
  title="Components/Core/Layout/Toolbar/Subcomponents/Block/Docs"
  of={BlockQuoteStories}
/>

<H1>Block components</H1>

The block typographic components are used inside another components or as a
base of them: `HelperMessage`, `ValidationMessage`, `FieldLabel`... etc.

<Source
  code={`
import { Typography } from '@devoinc/genesys-ui';
const { 
  BlockQuote, 
  Caption, 
  Code, 
  Heading, 
  Lead,
  List, 
  ListItem, 
  Paragraph 
} = Typography;
`}
/>

<H2>BlockQuote</H2>

`Typography.BlockQuote` renders an HTML Block Quotation Element: indicates that
the enclosed text is an extended quotation. A URL for the source of the quotation
may be given using the 'cite' attribute.

<H3>Basic usage</H3>

<Canvas of={BlockQuoteStories.BlockQuote} />

<H3>Props</H3>

<ArgTypes of={BlockQuoteStories} />

<H2>Code</H2>

`Typography.Code` displays its contents styled in a fashion intended to indicate
that the text is a fragment of computer code.

<H3>Basic usage</H3>

<Canvas of={CodeStories.Code} />

<H3>Props</H3>

<ArgTypes of={CodeStories} />

<H2>List and ListItem</H2>

`Typography.List` can render an HTML `<ul>` (by default) or `<ol>` which represents an
unordered/ordered list of items, typically rendered as a bulleted list.
Each child has to be a `Typography.ListItem` component, which renders an
HTML `<li>` element.

<H3>List - Basic usage</H3>

<Canvas of={ListStories.List} />

<H3>List - API</H3>

<ArgTypes of={ListStories} />

<H3>ListItem - Basic usage</H3>

<Canvas of={ListItemStories.ListItem} />

<H3>ListItem - API</H3>

<ArgTypes of={ListItemStories} />

<H2>Paragraph</H2>

`Typography.Paragraph` renders an HTML `<p>`. Paragraphs are usually represented
in visual media as blocks of text separated from adjacent blocks by blank lines,
but paragraphs can be any structural grouping of related content.

<H3>Basic usage</H3>

<Canvas of={ParagraphStories.Paragraph} />

<H3>Props</H3>

<ArgTypes of={ParagraphStories} />

<H2>Caption</H2>

`Typography.Caption` component is similar to `Paragraph` component, but it's oriented
to be used in short text blocks, so it's limited by default to one line and
'sm' size prop value.

<H3>Basic usage</H3>

<Canvas of={CaptionStories.Caption} />

<H3>Props</H3>

<ArgTypes of={CaptionStories} />

<H2>Lead</H2>

`Typography.Caption` component is oriented to be used as a subtitle or a
summary block next to a `Heading` component.

<H3>Basic usage</H3>

<Canvas of={LeadStories.Lead} />

<H3>Props</H3>

<ArgTypes of={LeadStories} />

<H2>Heading</H2>

`Typography.Heading` generates HTML headings which are the titles or subtitles
of our components: Panel Header titles, BoxMessage titles, Grouping titles
in Menus and forms... etc.

If we want the standard HTML headings (h1-h6) we have to use any 'h-' value for
type prop. They render an HTML `div` by default, but it's possible to use any
`h1` to `h6`, using the 'as' prop.

<H3>Basic usage</H3>

<Canvas of={HeadingStories.Heading} />

If we need extra-big headings for commercial or on boarding contexts, we can use
any 'hero-' value for 'type' prop:

<Source
  code={`
<Typography.Heading type={'hero-md'} >Heading</ Typography.Heading>
`}
/>

If we need a subtle heading to group menu items, form fields... etc. we can
use any 'overline-' value for 'type' prop:

<Source
  code={`
<Typography.Heading type={'overline-md'} >Heading</ Typography.Heading>
`}
/>

<H3>Props</H3>

<ArgTypes of={HeadingStories} />
