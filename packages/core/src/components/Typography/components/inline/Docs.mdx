import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as AbbrStories from './Abbr.stories';
import * as CodeInlineStories from './CodeInline.stories';
import * as DeletedStories from './Deleted.stories';
import * as HighlightedStories from './Highlighted.stories';
import * as InsertedStories from './Inserted.stories';
import * as ItalicStories from './Italic.stories';
import * as SmallStories from './Small.stories';
import * as StrongStories from './Strong.stories';
import * as SubStories from './Sub.stories';
import * as SupStories from './Sup.stories';
import * as UnderlinedStories from './Underlined.stories';

<Meta title="Components/Core/Text/Typography/Inline" component={AbbrStories} />

# [Inline components](#inline-components)

The inline typographic components are used inside another typographic block components.

They inherit the main text styles from those block components except their inline
specific styles: text-decoration, font-weight, vertical-align... etc.

<Source
  code={`
import { Typography } from '@devoinc/genesys-ui';
const {
  Abbr,
  CodeInline,
  Deleted,
  Highlighted,
  Inserted,
  Italic,
  Small,
  SrOnly,
  Strong,
  Sub,
  Sup,
  Underlined,
} = Typography;
`}
/>

## [Abbr](#abbr)

`Typography.Abbr` renders an HTML Abbreviation element (`<abbr>`) which represents an
abbreviation or acronym; the optional 'tooltip' prop can provide an expansion
or description for the abbreviation. If present, 'tooltip' must contain this
full description and nothing else.

### [Basic usage](#basic-usage)

<Canvas of={AbbrStories.Abbr} />

### [API](#api)

<ArgTypes of={AbbrStories} />

## [CodeInline](#codeinline)

`Typography.CodeInline` Displays its contents styled in a fashion intended to indicate that the text
is a short (and inline) fragment of computer code.

### [Basic usage](#basic-usage)

<Canvas of={CodeInlineStories.CodeInline} />

### [API](#api)

<ArgTypes of={CodeInlineStories} />

## [Deleted](#deleted)

`Typography.Deleted` renders an HTML `<del>` element, which represents a range of text that has
been deleted from a document. This can be used when rendering "track changes"
or source code diff information, for example.

### [Basic usage](#basic-usage)

<Canvas of={DeletedStories.Deleted} />

### [API](#api)

<ArgTypes of={DeletedStories} />

## [Highlighted](#highlighted)

`Typography.Highlighted` renders an HTML Mark Text element `<mark>` which represents text which is
marked or highlighted for reference or notation purposes, due to the marked
passage's relevance or importance in the enclosing context.

### [Basic usage](#basic-usage)

<Canvas of={HighlightedStories.Highlighted} />

### [API](#api)

<ArgTypes of={HighlightedStories} />

## [Inserted](#inserted)

`Typography.Inserted` renders an HTML `<ins>` element, which represents a range of text that has
been inserted or added to a document. This can be used when rendering
"track changes"or source code diff information, for example.

### [Basic usage](#basic-usage)

<Canvas of={InsertedStories.Inserted} />

### [API](#api)

<ArgTypes of={InsertedStories} />

## [Italic](#italic)

`Typography.Italic` renders an HTML `<em>` element which marks text that has stress emphasis.
The `Italic` component can be nested, with each level of nesting indicating a
greater degree of emphasis.

### [Basic usage](#basic-usage)

<Canvas of={ItalicStories.Italic} />

### [API](#api)

<ArgTypes of={ItalicStories} />

## [Small](#small)

`Typography.Small` renders an HTML `<small>` element which represents side-comments and small
print, like copyright and legal text, independent of its styled presentation.
It's also used as complementary info added to another typographic components.

### [Basic usage](#basic-usage)

<Canvas of={SmallStories.Small} />

### [API](#api)

<ArgTypes of={SmallStories} />

## [Strong](#strong)

`Typography.Strong` renders an HTML Strong Importance Element `<strong>` which indicates that its
contents have strong importance, seriousness, or urgency.

### [Basic usage](#basic-usage)

<Canvas of={StrongStories.Strong} />

### [API](#api)

<ArgTypes of={StrongStories} />

## [Sub](#sub)

`Typography.Sub` renders an HTML Subscript element `<sub>`, which specifies inline text which
is to be displayed as subscript for solely typographical reasons.

### [Basic usage](#basic-usage)

<Canvas of={SubStories.Sub} />

### [API](#api)

<ArgTypes of={SubStories} />

## [Sup](#sup)

`Typography.Sup` renders an HTML Superscript element `<sup>`, which specifies inline text which
is to be displayed as superscript for solely typographical reasons.

### [Basic usage](#basic-usage)

<Canvas of={SupStories.Sup} />

### [API](#api)

<ArgTypes of={SupStories} />

## [Underlined](#underlined)

`Typography.Underlined` renders as HTML Unarticulated Annotation element `<u>`,
which represents a span of inline text which should be rendered in a way that
indicates that it has a non-textual annotation.

### [Basic usage](#basic-usage)

<Canvas of={UnderlinedStories.Underlined} />

### [API](#api)

<ArgTypes of={UnderlinedStories} />
