import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from './DevoLogoLoader.stories';

<Meta of={Stories} />

# DevoLogoLoader

## Overview

The **AnimatedLogo** component is an animated logo loader that shows an
unknown time will be needed to be wait for the user because of an underlying
event is processing.

## How to import

<Source
  code={`
import { DevoLogoLoader } from '@devoinc/genesys-ui';
`}
/>

## Basic usage

<Canvas of={Stories.Basic} />

## API

<ArgTypes of={Stories} />
