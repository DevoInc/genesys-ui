import { Meta, Canvas, ArgTypes, Source, Markdown } from '@storybook/blocks';

import * as Stories from './Flex.stories';

<Meta of={Stories} />

# Flex

Component designed to create stacked groups of child components, with the
flexibility to distribute the children in a single row, a column with the
children stacked vertically, or in a mixed wrapped structure with rows and
columns.

The Flex component allows us to create any flex structure using CSS flex
properties such as 'inline' (which uses display inline-flex instead of display
flex), flex-direction, justify-content, align-items, align-content, and
flex-wrap. To separate each child component, Flex.Item props are used.

For more information on CSS flex, refer to
[A Complete Guide to Flexbox | CSS-Tricks](https://css-tricks.com/snippets/css/a-guide-to-flexbox/).

## How to import

<Source code={`import { Flex } from '@devoinc/genesys-ui';`} />

## Basic usage

<Canvas of={Stories.Playground} />

## Using the as property

The `as` property allow these components to be rendered as any HTML tag:
'div', 'li', 'section'... etc.

If you want to use the `Flex` component rendered with a different HTML tag, you
can use the `as` property.
It allows these components to be rendered as any HTML tag, such as div, li,
section, and so on.

<Source
  code={`
import * as React from 'react';
import { Flex } from '@devoinc/genesys-ui';

const MyFlexCmp = () => (

{' '}

<Flex as={'ul'}>
  <Flex.Item as={'li'}>Child 1</Flex.Item>
  <Flex.Item as={'li'}>Child 2</Flex.Item>
  <Flex.Item as={'li'}>Child 3</Flex.Item>
</Flex>
); `} />

## Using Flex.Item

We can use `Flex.Item` component in a flex structure wrapping children to assign
flex, order, and align-self CSS styles to each child component. It's only
necessary when we need those specific styles for any child.
Here is an example that demonstrates different CSS flex and align-self
settings for each child component of a Flex component:

<Canvas of={Stories.FlexAndFlexItems} />

## Related components

- [Flex.Item](?path=/docs/components-layout-flex-components-flexitem--overview)
- [HFlex](?path=/docs/components-layout-flex-hflex--overview)
- [VFlex](?path=/docs/components-layout-flex-vflex--overview)
- [Wrap](?path=/docs/components-layout-flex-wrap--overview)

## Props

<ArgTypes of={Stories} />
