import { Meta, Canvas, ArgTypes, Source, Markdown } from '@storybook/blocks';
import { Heading } from '../../../../../../.storybook/docs/blocks';

import flexComponent from './partials/flexComponent.md?raw';
import * as Stories from './Flex.stories';
import * as CaseStories from './Flex.cases.stories';

<Meta of={Stories} />

<Heading.H1>Flex</Heading.H1>

<Heading.H2>Overview</Heading.H2>

<Markdown>{flexComponent}</Markdown>

<Heading.H2>How to import</Heading.H2>

<Source
  code={`
import { Flex } from '@devoinc/genesys-ui';
`}
/>

<Heading.H2>Basic usage</Heading.H2>

<Canvas of={Stories.Base} />

<Heading.H2>Cases</Heading.H2>

<Heading.H3>Using the as property</Heading.H3>

The `as` property allow these components to be rendered as any HTML tag:
'div', 'li', 'section'... etc.

If you want to use the `Flex` component rendered with a different HTML tag, you can use the `as` property.
It allows these components to be rendered as any HTML tag, such as div, li, section, and so on.

<Source
  code={`
import * as React from 'react';
import { Flex } from '@devoinc/genesys-ui';

const MyFlexCmp = () => (

{' '}

<Flex as={'ul'}>
  <Flex.Item as={'li'}>Child 1</Flex.Item>
  <Flex.Item as={'li'}>Child 2</Flex.Item>
  <Flex.Item as={'li'}>Child 3</Flex.Item>
</Flex>
); `} />

<Heading.H3>Flex.Item props</Heading.H3>

We use each Flex.Item prop in a flex structure to assign flex,
order, and align-self CSS styles to each child component.
Here is an example that demonstrates different CSS flex and align-self
settings for each child component of a Flex component:

<Canvas of={CaseStories.FlexAndFlexItems} />

<Heading.H2>Props</Heading.H2>

<ArgTypes of={Stories} />

<Heading.H2>Related components</Heading.H2>

- [Flex.Item](?path=/docs/components-core-layout-flex-subcomponents--docs)
