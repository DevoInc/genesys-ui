import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from './CheckboxControl.stories';

<Meta of={Stories} />

# CheckboxControl

Form element that allows users to make a binary choice, i.e. a choice between
one of two possible mutually exclusive options.
By default, when a checkbox is selected, it displays a checkmark or a tick
inside a square or a circle, indicating that the option has been chosen.

## How to import

<Source code={`import { CheckboxControl } from '@devoinc/genesys-ui';`} />

## Basic usage

<Canvas of={Stories.Playground} />

## Uncontrolled

When the checked behavior depends solely on the HTML type checkbox input.

<Canvas of={Stories.Uncontrolled} />

## Controlled

When the checked behavior depends on a state.

<Canvas of={Stories.Controlled} />

## Indeterminate

<Canvas of={Stories.Indeterminate} />

## Related components

- [Checkbox](?path=/docs/components-form-checkbox--docs)
- [SelectControl](?path=/story/components-form-selectcontrol-cases--multiple-subtle)
- [Table bulk actions](?path=/docs/components-layout-table-actions-bulkactions-by-index--overview)

## Props

<ArgTypes of={Stories} />
