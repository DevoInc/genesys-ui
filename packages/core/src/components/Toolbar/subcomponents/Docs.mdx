import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from '../Toolbar.stories';
import * as GroupStories from './ToolbarGroup/ToolbarGroup.stories';
import * as ItemStories from './ToolbarItem/ToolbarItem.stories';
import * as SeparatorStories from './ToolbarSeparator/ToolbarSeparator.stories';

<Meta
  title="Components/Core/Layout/Toolbar/Subcomponents/Docs"
  of={GroupStories}
/>

<H1>Subcomponents</H1>

<H2>How to import</H2>

<Source
  code={`
import { Toolbar } from '@devoinc/genesys-ui';
`}
/>

<H2>Toolbar.Group</H2>

The `Toolbar.Group` component is used to split the toolbar component in different sections.

<H3>How to use</H3>

<Source
  code={`
<Toolbar>
  <Toolbar.Group {...args}>
    <Toolbar.Item>One</Toolbar.Item>
  </Toolbar.Group>
  <Toolbar.Group>
    <Toolbar.Item>Two</Toolbar.Item>
  </Toolbar.Group>
</Toolbar>
`}
/>

<H3>Basic usage</H3>

<Canvas of={GroupStories.Group} />

<H3>Props</H3>

<ArgTypes of={GroupStories} />

<H2>Toolbar.Item</H2>

The `Toolbar.Item` component is used to create items to be used inside a `Toolbar.Group`.

<H3>How to use</H3>

<Source
  code={`
<Toolbar>
  <Toolbar.Group {...args}>
    <Toolbar.Item>One</Toolbar.Item>
    <Toolbar.Item>One</Toolbar.Item>
  </Toolbar.Group>
</Toolbar>
`}
/>

<H3>Basic usage</H3>

<Canvas of={ItemStories.Item} />

<H3>Props</H3>

<ArgTypes of={ItemStories} />

<H2>Toolbar.Separator</H2>

The `Toolbar.Separator` component is used to create a separator mark between items.

<H3>How to use</H3>

<Source
  code={`
<Toolbar>
  <Toolbar.Group>
    <Toolbar.Separator />
  </Toolbar.Group>
</Toolbar>
`}
/>

<H3>Basic usage</H3>

<Canvas of={SeparatorStories.Separator} />
