import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from './Helper.stories';

<Meta of={Stories} />

# Helper

## Overview

The helpers give information to the users in order to fill in a field or a form,
configure a functionality... etc. and also provide feedback about
the status of processes or configurations.

## How to import

<Source
  code={`
import { Helper } from '@devoinc/genesys-ui';
`}
/>

## Default

<Canvas of={Stories.Base} />

## API

Props of the VFlex component

<ArgTypes of={Stories} />
