import { Meta, Canvas, ArgTypes } from '@storybook/blocks';

import * as ActionsStories from './AppBarActions/AppBarActions.stories';
import * as ContainerStories from './AppBarContainer/AppBarContainer.stories';
import * as DividerStories from './AppBarDivider/AppBarDivider.stories';
import * as HeadingStories from './AppBarHeading/AppBarHeading.stories';
import * as NavigationStories from './AppBarNavigation/AppBarNavigation.stories';
import * as OptionsStories from './AppBarOptions/AppBarOptions.stories';

<Meta title="Components/Core/Navigation/AppBar/Subcomponents/Docs" />

# Subcomponents

Set of subcomponents that can be used to build a `AppBar` component.

## Container

The `AppBar.Container` component is used to define the main container of the `AppBar`.

### Basic usage

<Canvas of={ContainerStories.Container} />

### Props

<ArgTypes of={ContainerStories} />

## Navigation

The `AppBar.Navigation` component is used to list the tabs items.

### Basic usage

<Canvas of={NavigationStories.Navigation} />

### Props

<ArgTypes of={NavigationStories} />

## Heading

The `AppBar.Heading` component is used to define a `AppBar` heading fixed at the left side.

### Basic usage

<Canvas of={HeadingStories.Heading} />

### Props

<ArgTypes of={HeadingStories} />

## Divider

The `AppBar.Divider` show a `Divider` component to separate diferent components.

### Basic usage

<Canvas of={DividerStories.Divider} />

## Actions

The `AppBar.Actions` component is used to align to the right side of the `AppBar`.

### Basic usage

<Canvas of={ActionsStories.Actions} />

### Props

<ArgTypes of={ActionsStories} />

## Options

The `AppBar.Options` component is used to align to the right side of the `AppBar`.
It's the same of `AppBar.Actions` components but adding a `Separator` at the beging.

### Basic usage

<Canvas of={OptionsStories.Options} />

### Props

<ArgTypes of={OptionsStories} />
