import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';
import { Heading } from '../../../../../.storybook/docs/blocks';

import * as Stories from './DateTimeRangePicker.stories';

<Meta of={Stories} />

<Heading.H1>DateTimeRangePicker</Heading.H1>

The DateTimeRangePicker is an extension of the DateTimeRange component.
It renders a date type input which shows the aforementioned DateTimeRange component when clicked, allowing users to select a date and time range in several ways.

<Heading.H2>How to import</Heading.H2>

<Source
  code={`
import { DateTimeRangePicker } from '@devo/genesys-ui-datetime';
`}
/>

<Heading.H2>Basic usage</Heading.H2>

<Canvas of={Stories.Base} />

<Heading.H2>Props</Heading.H2>

<ArgTypes of={Stories} />
