import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from './DateTimePicker.stories';

<Meta of={Stories} />

# DateTimePicker

The `DateTimePicker` is an enhancement of the `DateTime` component,
designed to offer extended functionality.
It presents a date type input field and the `DateTime` component,
enabling users to conveniently select a date and time through various means.

Users can manipulate the value either directly from the `DateTime` component
or via `DateTimeInput`component (input text field).

Each time the input text field value is modified, it undergoes validation
against accepted date formats.
The first value in the `dateFormats` array serves as the default value to display.
If no format is specified, the default value will be `YYYY-mm-dd HH:mm:ss`.

When the `Enter` or `Escape` key is pressed, or when focus is lost,
the `onChange` method of the component is triggered.

Any change made to the `DateTime` component triggers the `onChange` method.

## How to import

<Source code={`import { DateTimePicker } from '@devo/genesys-ui-datetime';`} />

## Basic usage

<Canvas of={Stories.Base} />

## Related components

- [Calendar](?path=/docs/components-datetime-calendar--docs)
- [DateTimeInput](?path=/docs/components-datetime-datetimeinput--docs)
- [DateTime](?path=/docs/components-datetime-datetime--docs)

## Props

<ArgTypes of={Stories} />
