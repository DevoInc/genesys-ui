import { Meta, Canvas, Source, ArgTypes } from '@storybook/addon-docs/blocks';

import * as Stories from './Time.stories';

<Meta of={Stories} />

# Time

## How to import

<Source code={`import { Time } from '@devo/genesys-ui-datetime';`} />

## Basic usage

<Canvas of={Stories.Playground} />

## `minDate` & `maxDate`

Limit the selection between these two limits, those are applied to the native
`min` and `max` properties of the `input` type `time`, so the representation of
these limits are done by the browser native element

<Canvas of={Stories.Limits} />

## I18n

- `i18n.time`: For accessibility

## Props

<ArgTypes of={Stories} />
