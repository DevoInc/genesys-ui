import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from './Calendar.stories';

<Meta of={Stories} />

# Calendar

The Calendar is a component designed to work with dates.
It allows users to select a single date or a range of dates within a particular month.
The calendar displays the days of the month in a grid format.

## How to import

<Source
  code={`
import { Calendar } from '@devo/genesys-ui-datetime';
`}
/>

## Basic usage

<Canvas of={Stories.Base} />

## Props

<ArgTypes of={Stories} />
