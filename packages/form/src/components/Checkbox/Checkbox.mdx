import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';

import * as Stories from './Checkbox.stories';

<Meta of={Stories} />

<H1>Checkbox</H1>

Form element that allows users to make a binary choice, i.e.
a choice between one of two possible mutually exclusive options. It provides a range of customization
options to control the appearance and behavior of the switch,
as well as additional props for accessibility and guidance.

Checkbox is a wrapper over the [CheckboxControl](?path=/docs/components-core-form-checkboxcontrol--docs)
component, and uses the [Field](?path=/docs/components-core-form-field--docs)
component to provide additional functionality such as labelling, helper error messages, and validation.

<H2>How to import</H2>

<Source
  code={`
import { Checkbox } from '@devo/genesys-ui-form';
`}
/>

<H2>Basic usage</H2>

<Canvas of={Stories.Base} />

<H2>Props</H2>

<ArgTypes of={Stories} />

## Related components

- [Checkbox](?path=/docs/components-core-form-checkboxcontrol--docs)
