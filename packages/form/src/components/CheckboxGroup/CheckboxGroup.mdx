import { Meta, Canvas, Source, ArgTypes } from '@storybook/blocks';
import { Heading } from '../../../../../.storybook/docs/blocks';

import * as Stories from './CheckboxGroup.stories';

<Meta of={Stories} />

<Heading.H1>CheckboxGroup</Heading.H1>

Component used to render a group of checkboxes with customizable properties.
The component takes in a set of child Checkbox components as its children prop,
and allows for additional customization through props like direction (which determines whether the checkboxes are arranged vertically or horizontally),
legend (which provides a label for the group of checkboxes), or status.

<Heading.H2>How to import</Heading.H2>

<Source
  code={`
import { CheckboxGroup } from '@devo/genesys-ui-form';
`}
/>

<Heading.H2>Basic usage</Heading.H2>

<Canvas of={Stories.Base} />

<Heading.H2>Props</Heading.H2>

<ArgTypes of={Stories} />
